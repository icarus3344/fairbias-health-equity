# Failed-task recovery gate — 2026-09-17

User authorizes supervisor-led repairs, subagents, and bounded existing-server pilots before renting another CPU. This gate does not change the active 70-file registration or overwrite any old run. Frozen inherited baseline remains protected. No stage/commit.

## Scope and admission

The metadata-only inventory contains 1,242 failed/incomplete seed jobs: EG 23, LFR 960, BM 198, BM_AE 21, Joint 40. LFR represents 120 shared representation keys; BM represents nine. Corrected FRAPPÉ 480 jobs are an independently identified, already-running queue and are not part of this failure manifest.

1. **EG:** Preserve trained reductions and fractional decision-probability semantics. Validate convex weights and both PMF columns, repair only bounded endpoint floating-point roundoff. Reject material violations. Pilot all six affected candidate configurations at seed 0 using F/C. Formal rerun must apply this deterministic wrapper consistently across the EG comparison family, or prove unchanged outputs for reused valid records before combining.
2. **LFR:** Keep the AIF360 objective, feature preprocessing, initialization, prototype count, penalties, seeds, bounds, maxfun and maxiter. Replace finite differences with the reviewed piecewise analytic derivative. Verify upstream source hashes, loss equality, gradients, RNG and reload. Pilot original failed k=10/Az=.1 arm003 plus k=5/Az=50 arm001/004. Preserve nonsmooth derivative conventions and projected-gradient diagnostics. SciPy termination is not a global optimum certificate. Full rerun uses 120 representation fits with a separate runtime/cache identity, then 960 downstream seed jobs.
3. **BM:** Reproduce the exact intermediate geometry failure on F, recording only aggregate geometry/state diagnostics. Distinguish empty feature exhaustion, undefined geometry and MDS iteration cap. Never map an undefined/empty geometry to zero bias. Feature exhaustion may require an explicit infeasibility outcome rather than a successful model. Retry MDS only under a versioned, predeclared larger-cap policy; preserve initial failed attempts and verify the retried embedding.
4. **BM_AE / Joint:** Reuse verified exact NumPy MDS acceleration. Propagate geometry errors that the legacy AE path may swallow. Explicitly distinguish complete search, feasible budget-limited incumbent, incomplete geometry and failed fit. Increased budgets and scheduled Joint are separate algorithm/budget variants and cannot silently replace the original strict Joint in a paper table.

## Pilot controls

Only development fitting F and calibration C may be accessed. Prepared F/C/S containers are hash verified; unused S is immediately discarded, with no S/T predictions, metrics, rankings or tuning. No local NHIS fits. Separate isolated processes, numerical threads fixed to one, CUDA disabled, nice 19, fresh output directory, external wall budget and sampled 4 GiB RSS threshold per pilot. RSS sampling is not a kernel memory cap. At most two pilot cores while current formal queues occupy the other cores; resource scheduler coordinates ownership before any expansion.

Every pilot records original job/config/seed/input/source identities, additive runtime source hashes, dependency versions, actual termination, elapsed time/RSS and exact model reload where a completed model exists. A supervisor exit receipt alone is not success. No old cache is read for a repaired representation.

## Release requirements

Independent synthetic and Linux pilot verification; original 70-file source manifest remains unchanged on both hosts. Prepare exact rerun ownership manifest with runtime/cache identifiers and explicit terminal non-success outcomes. Preserve unsuccessful attempts in denominator/accounting. Report supported and unresolved families separately. Estimate new CPU cost from completed representative fits and actual effective 32-core / 60-GiB limits. Rent/launch a new node only once there is validated, runnable work; current nodes may continue bounded pilots and already-authorized queues.

No fairness superiority or publication-ready claim is permitted from repair pilots. Final paper selection/temporal evaluation/survey uncertainty is a subsequent gate after consistent candidate-family coverage and runtime-aware aggregation.

## CPU continuation admission — 2026-09-17 13:30 Asia/Shanghai

The LFR960 and EG23 queues are terminal and independently verified. The supervisor releases a bounded CPU-only diagnostic batch: all eight distinct BM undefined-geometry representation keys, with deterministic minimum-job selection from the frozen failure inventory, and a fresh repetition of BM_AE `3c8badc6d7eeaa99adce_s0` using the already declared cap10-to40 policy. BM receives900s per root; BM_AE receives7200s for the combined fresh attempts, replacing only the prior insufficient1800s external wall allowance. Internal geometry/utility/BM budgets, input data, seeds and objectives remain unchanged. Old attempts are retained. This is an explicitly identified wall-budget sensitivity pilot, not silent original-budget completion.

With no remaining formal workers, the diagnostic batch may use nine independent single-thread children on the32-core/60-GiB CPU host, each with the existing sampled4-GiB RSS threshold. It uses a fresh `artifacts/nhis/fairbias_followup_20260917/` namespace and source/input-bound launch and exit receipts. This release does not authorize GPU jobs or local real-data fits. It does not admit an unsuccessful geometry path or partial feasible incumbent as a successful model.

Formal BM22 MDS-retry and strict Joint38 original wall-failure recovery may be independently released after the new worker/queue contracts pass supervisor review and Linux synthetic integration. Keep method/budget policy identifiers separate and retain all failures. Joint2 MDS-retry and BM_AE formal recovery remain unreleased pending their own pilot admission. Coordinate any simultaneous formal queue with active diagnostic children and memory; do not treat absence of old production workers as absence of pilots. No S/T rankings are used for these decisions.
