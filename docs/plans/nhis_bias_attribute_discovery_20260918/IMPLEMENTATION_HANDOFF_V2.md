# 新窗口执行提示词 v2：系统发现NHIS候选公平审计属性O

请在 /Users/lkc/Downloads/code_v_0_3 按导师最新澄清继续研究：先把NHIS做深入，系统查找除了既有性别、族裔、残疾之外，还有哪些有领域意义的属性O能揭示被忽略的模型差异；发现稳定现象后比较已跑通的方法，必要时针对少数新O训练，再选择相关数据集复核。FairBias是比较方法，不必证明它最好。

首先阅读docs/AI_EXECUTION_PROTOCOL.md，以及：
- docs/plans/nhis_bias_attribute_discovery_20260918/RESEARCH_PLAN_V2.md
- docs/plans/nhis_bias_attribute_discovery_20260918/CANDIDATE_O_DRAFT.json
- docs/plans/nhis_bias_attribute_discovery_20260918/planning_verification.json
- docs/reports/FAIRBIAS_COMPLETION_FORMAL_EVALUATION_ACCEPTANCE_20260918.md
- docs/paper/manuscript_readiness_20260918/READINESS_REPORT.md

本版优先于上一份nhis_hidden_bias_research_20260918计划。取消“性别×残疾四组+最多10规则组”为默认主线，取消旧共同完整案例域及旧K/2K+2检验家族；严格保留数据隔离、源绑定、调查统计和2025先冻结后开放等底线。不要同时执行互相冲突的两个方案。

请实际推进A0–A6，先完成A0–A2再进行真实性能扫描，主管自主验收常规阶段，无须逐轮询问。不要仅写下一份计划后结束。必要时可委托具体独立子任务，但主管亲自核验，不空转、不循环监督。

第一阶段任务：
1. 基于官方元数据构建全NHIS变量地图；不要只扫描现有21预测列。区分公平锚点、社会处境、临床背景、数据生成状态。核对跨年构念、类别、适用人群、路由、来源、O与X/Y关系及文献。种子JSON仅含本地注册的24项，未做官方跨年准入。
2. 冻结至多30项候选O及分组/参照/准入规则，不依据模型误差挑切点；排除ID、设计变量、输出/误差、Y和近似复述Y的变量。相关/嵌套O不当作独立发现。
3. 盘点既有冻结预测与模型；核验记录顺序及哈希，补推断需独立版本，不默默重训。先用原无干预GBDT发现，LR敏感性；已有公平方法可用于迁移审计，准确记录O_train和O_audit。
4. 每个O有自己的有效域，同O下所有方法同域；不能要求全候选O都非缺失。禁止把q当p做校准，禁止把多种子当独立人，禁止IID替代复杂调查设计。
5. 建立新的隔离审计模块与合成合同测试；不要通过修改旧四臂入口、松动NA或哈希校验来硬接新O。
6. 2023发现、3原锚点+最多5新O冻结、2024回顾性复核。2024已知，不能伪造未见测试。O扫描家族和选后检验问题按v2方案处理，不能只报告显著项。
7. 合格2025仍需访问历史和变量兼容审计。未冻结所有模型/O/指标/检验族和评价源码前禁止微观数据评价。最多3个新O的针对性训练若实施，必须发生在2025开放前；否则只能算新的探索。
8. 无法证明因果时用“沿该O观察到差异”，不说“该特征导致bias”。SHAP大/人群风险高不等于不公平。跨数据集任务须在发现后核对可比性，不直接挪用不等价结局。
9. 输出属性图谱、跨年森林图、方法改善矩阵、已知/新发现/负结果及局限、Methods/Results草稿和导师汇报材料。

当前仅做了计划修订，没有新增训练或数据分析；80个完成版FairBias及既有269模型复用资产保持原样。所有服务器保持关闭，自动任务不启用，默认本地2进程、BLAS1；实际计时后有依据才增至4。新增收费资源先给具体任务与预算，不能擅自启动。

保留工作树、受保护历史文件、数据和原产物；不reset、不提交或上传，不输出个体记录。新产物使用独立版本路径，例如artifacts/nhis/bias_attribute_discovery_<run_id>/与docs/reports/nhis_bias_attribute_discovery_<run_id>/。JSON草案不是冻结方案，需补全官方映射、model_id、源/输入哈希、统计家族和独立验收证据才能freeze。

需要人类事实（应用场景、作者责任、伦理/数据权限）时集中询问并说明缺口；常规可逆实现和内部验收自行推进。遇到失败保留证据、阻塞相关依赖，不无意义重跑已有成功模型。

