# 元数据扫描加速与 CPU 接续

2026-09-17 17:25，主管执行。训练结果和模型均不改变；此次优化仅涉及分析阶段的 JSON 凭证读取。

## 实测瓶颈与修改

旧准备进程17:10仍占用约一个CPU核，累计1420.4 CPU秒、RSS约719MB。打开的文件是结果JSON，累计读入字节约8.37GB而物理读盘为0，说明缓存已命中。对一个356497字节的真实凭证做性能剖析，逐字符peek/take/value产生约341万次调用，构成主要解析开销；没有解码其S/T指标。

新增 catalog_json_reader 使用正则表达式按字符串和数字块检查JSON语法；只解码调用方明确选择的字段。被跳过的指标和诊断不会交给JSON解码器。64MiB以上文件、缓冲超限和需要保持错误先后次序的Unicode异常回退到原流式实现。保留重复键、非法数字/转义、嵌套深度和字段选择契约。新reader源码也加入S冻结的分析源码指纹。

主管独立验证：本地253测试通过47.86秒，服务器253测试通过58.95秒。包括原目录/选模/推断/评价/报告测试、超过一万次生成数据差分，以及排除字段不解码、UTF-8/深度/超限回退检查。一个真实凭证新旧解析结果完全相同；无剖析计时为旧0.31817秒、新0.05286秒，约6.02倍。这是单文件解析速度，不能当作整个流程的加速比。全部9166个结果文件总计约1.17GB，最大约11.1MB；目录约21.1MB，均在快速路径大小范围内。

## 不影响选模的验证

旧流程已生成S冻结，900模型文件，SHA256 d935a5b688bf2e0a7c69aae2d51d8b7d4f83cd5e90b59011770dec534d914e4c。在旧流程继续进行研究冻结时，主管安全停止了该只读准备进程，保留全部旧文件，没有停止或重新进行模型训练。

新准备进程PID771847于17:25:33启动，使用 main_preparation_v2 和 main_analysis_v2 独立目录。它必须先证明完整重建目录与原绑定目录完全一致，再重算原规则S冻结，并要求整个新旧选模产物除analysis_source_files外逐字段完全相等。任何差异都会阻止研究冻结和T释放。原900模型不是因新解析速度而重新挑选的科学候选。

2024输入已以不解析行内容的方式传至CPU并核验SHA256；传输不授权评价。新研究冻结仍需主管释放，之后按完整臂执行预测、2000次调查PSU复制及配对区间。

## CPU 使用与停机条件

当前准备和接下来的跨年评价仍使用既有CPU，不需要新增机器或GPU。先完成一臂记录实际评价耗时/内存，再并行执行其他完整臂；不同模型共用调查复制的口径保持不变。随后汇总、验收并备份主分析及已登记补充分析，才可停止CPU实例。论文写作和读取已备份的聚合表图可以在本地继续，无须为写作持续租用CPU。

当前没有可靠的整批实测耗时，不能据单个JSON或合成统计基准给出准确关机时刻。下一次更新时间点是新冻结完成或首臂评价完成。

证据：fairbias_recovery_20260917/evidence/fast_reader_supervisor_review_20260917.json、fast_reader_deployment_20260917.json、main_preparation_v2_launch_20260917.json、t2024_transfer_20260917.json。
