# FairBias 主方法完整落实：本地软件准入通过，真实数据试跑待服务器恢复

用户要求完整实现并运行 BM+AE / Joint，接受增加计算投入。先前把旧预算实验结束视为可收尾、把未完成的主方法继续列为补充研究，不能满足这一目标。本次按新的完整落实计划执行；不宣称训练已补完。

## 当前失败的真实含义

主管逐一核对 catalog 绑定及实际结果文件 SHA256，覆盖当前 Joint38 和 AE40 的两个失败结果，共40个回执。

| 集合 | 已核实情况 | 处理方向 |
|---|---|---|
| strict Joint 恢复38 | 13有效、18 AE提交上限、2 MDS上限、5墙时终止 | 扩展搜索/MDS预算；取消固定墙时失败判定 |
| 未进入38项恢复的原Joint2 | Arm002、LR/GBDT、seed0的MDS失败 | 两项都纳入新40项完整族 |
| AE40 | 38有效；最后2项均为MDS上限，未触发AE40重试 | 修复几何求解预算，而不是再假定AE40不够 |
| BM失败198 | 8个空/无效几何根及其依赖176项；1个MDS根及其依赖22项 | 另做9根诊断，修复后覆盖依赖；当前不能宣称已解决 |

证据：`scratch/fairbias_completion_20260917/supervisor_failure_verification.json`。预算上限不是模型不具有预测能力的证据，也不等于搜索完成。无可接受候选或未定义表示仍须具体诊断，不能通过允许非有限几何或虚构模型消除。

## 已实现的改进

1. `src/nhis_fairbias/benchmark/fairbias_completion.py`：新的独立运行版本，从AE40开始。仅在精确匹配预算触顶时倍增相应预算，以同一随机种子重放；保持候选顺序、效用和几何条件。MDS触顶则提高迭代上限，并以相同种子重跑全部初始化。没有固定重试次数或墙时截止。
2. `src/nhis_fairbias/benchmark/mds_completed_cache.py`：保存完成且通过现有审计的小矩阵MDS结果。输入、完整参数、源码、数值环境绑定，RAM和磁盘复用；部分写入、中断、损坏、未知环境均不伪造命中。缓存保持逻辑预算计数；它不是完整搜索状态或未完成SMACOF迭代的检查点。
3. `scripts/run_nhis_fairbias_completion.py`：只读原登记，构建完整80项清单并提供独立F/C试跑入口。覆盖BM+AE40和Joint40，不遗漏原Joint2；保存模型、校准策略和重载证据。中断保留为待接续；无S选择或T评价入口。

原注册的70文件完全匹配旧哈希，旧AE、enhancement和已验收内核未修改。新运行98文件另行绑定。新预算可能得到与旧上限下不同的模型，必须标为新计算政策，不能冒充旧冻结结果。

## 独立验证

主管亲自运行相关7个测试文件：**304 passed, 218 warnings in 35.34s**。警告主要为固定版本sklearn弃用提示及明确测试的回退情况；无失败。日志：`scratch/fairbias_completion_20260917/supervisor_tests.log`。

覆盖实际BM+AE/Joint合成搜索、真实类别合并、正收益AE幂变换、AE/几何预算触顶后继续、最终轨迹及预测字节一致、模型重载、跨进程缓存复用、源与环境变化、坏缓存、中断、80项覆盖与输入哈希防护。新入口用纯合成F/C完整经过计划→拟合→校准→保存→重载；没有拟合真实NHIS，没有重新读取S/T指标。

子代理另测小矩阵缓存性能：24节点/5维时普通MDS中位18.38ms，冷缓存20.14ms，RAM命中0.88ms，磁盘命中1.12ms；8节点/2维为3.45/4.90/0.87/1.09ms。此为合成微基准，不是整套NHIS加速倍数。首次无复用时存在开销。磁盘缓存尚无自动淘汰，运行中须核对空间；失效环境重新计算，不能跨不兼容数值环境盲目复用。

## 已冻结的部署物与下一步

目录：`artifacts/nhis/full_method_completion_20260917/control/`。

- `manifest.json`：80项；内容seal `ad9d9e3fa8126b9daf47c62b7908dda47b0bf3f220aacc9b70e4f2176aada5bb`。
- `pilot_plan.json`：按失败类型与覆盖固定10项，包括AE40剩余2、Joint原MDS2及恢复MDS2、AE上限代表、墙时代表、原成功对照。没有按S/T性能筛选。
- `runtime.tar.gz`：460921字节；SHA256 `e061145e17e502b7a78b7eb9501315c8eb01bb6e64f7a87a3d8db8ed71f40833`。包含源码、测试和清单，不包含NHIS数据。
- `local_acceptance.json`：软件准入、文件哈希和未执行边界。

用户确认原CPU已关机、实例和磁盘仍保留。本次未连接任何服务器。软件现已可用于服务器试跑，已请求用户开回原32核CPU并确认SSH是否变化；无需先重新租GPU。恢复后先核对CPU/内存配额、剩余磁盘、依赖和冻结输入，在独立部署目录验收Linux测试，再运行10项F/C代表。初始2并发，观察实际RSS后调整；不把固定墙时当实验终点。真实耗时/峰值内存测得后，再给全族工期和资源配置。

后续必须完成：代表错误修复、完整80项同政策训练验收、BM9根及依赖闭环、独立版本选择/冻结/调查评价、论文解释与图表。80项是原固定锚点集合，不代表与BM一样全面的超参数搜索；用于主方法比较的扩展网格和调参预算还须在读取新S/T表现前统一登记。不能仅完成80个锚点就宣称主方法已获得充分调参的最优表现。

2024 T已知，新预算登记属于在测试结果已知后的计算完成扩展；修复依据限定失败/F/C证据。旧925模型和既定表图保留。最终报告可以出现FairBias不优于对照的结果，但不能以未完成主方法作为默认交付。

自动任务配置本次只读看到为PAUSED；未擅自恢复。待用户恢复服务器后按有界任务推进，不承诺当前已有后台训练或自动继续。
