# WestB v2：修复缓存排序并启动完整80项F/C训练

用户明确要求“修复后部署训练”。连接 `connect.westb.seetacloud.com:35430`，使用用户已安装的专用SSH密钥。新实例实际为64CPU配额、128849018880字节内存（120GiB）、50GB数据盘；未访问已退休GPU或原关机CPU，也未终止其他进程。

## 修复与独立验收

首版Linux验收为303通过、1失败。两次独立进程使用完全相同的NumPy/Scipy/OpenMP库及线程数，`threadpool_info()`返回列表的顺序不同，导致环境指纹不同。数值结果一致，但第二进程不命中磁盘缓存。

`src/nhis_fairbias/benchmark/mds_completed_cache.py`版本升为`mds_completed_cache_v2`，在构造环境指纹前按完整JSON规范化排序库记录。保留所有记录、重复项、库SHA、架构和线程参数；不取消环境检查或放松数值条件。新增测试证明枚举顺序不影响身份，真实架构改变仍改变身份，多线程仍拒绝缓存。旧源码及首版失败日志保存在旧部署目录和原打包文件中。

新队列增加受控阶段推进：先10项F/C代表全部完成且模型/回执核验通过，再释放其余70项。所有80项用同一清单与计算政策；前10项只计一次，不重新拟合，也不替换旧研究结果。某代表失败则保留记录、停止后70项派发并等待修复；不把超时/数值失败转为成功。

验证证据：

- 本地缓存及适配入口：55 passed；本地调度契约：8 passed。
- Linux正式部署回归：**311 passed, 218 warnings in93.33s**。
- Linux追加真实调度循环合成测试：**2 passed, 6 deselected in0.29s**，验证80项无重复，以及代表失败时阻断70项。
- 25项安装依赖的wheel哈希与前一服务器安装报告匹配；没有安装GPU训练栈来执行这些LR/GBDT任务。
- 原70登记源/配置仍匹配；新运行99文件单独绑定。四臂准备数据分别核对登记SHA，没有重新划分、预处理、查看新S/T性能或修改超参数。

## 已启动队列

远程根目录：`/root/autodl-tmp/fairbias_completion_v2_20260917`。

运行：`runs/full80_v2`；控制器PID2077。启动时前两个进程PID2078/2079，对应Arm002种子0的LR和GBDT BM+AE任务。

启动约47秒的独立快照：两个任务各消耗约46.8CPU秒、各1线程、RSS约268MiB；总cgroup内存约1.96GiB。进度事件显示真实MDS触及10000迭代后按新政策扩展。此证据说明正在训练，不代表已成功停止或得到最终预测表现。

据该快照，将请求并发从2提高到56；控制器代表阶段最多10并发，全族阶段最多56并发。内存达到cgroup上限80%或数据盘可用不足5GiB时停止新派发。已有进程不移动、不因固定墙时被杀；这些守卫不保证进程绝不OOM，实际峰值仍须关注。没有S/T评价入口，`formal_benchmark_admission`仍为false，全部F/C模型将由主管独立验收后用于新版本评价。

清单seal：`6607256f5efa027e0c954e7e0a0021d886074bf6dbfde0a52f1e5c5ba69ba0f0`。

包SHA256：`8e001d87b5af2008393932e6e313dbe82f44285d5e3d5346272fb321e56e1ac3`。

控制器SHA256：`98523cfed4def81ad03a3cf7d3f6fcb200e01b4420c4cf4bc299e28fe23902b4`。

本地证据目录：`artifacts/nhis/full_method_completion_20260917/westb_35430/v2/control/`。其中包含原始Linux日志、部署登记、启动回执和打包信息。最终在途快照另存`postlaunch_snapshot.json`。

## 继续边界

先读取运行的`state.json`、`queue_receipt.json`及失败进程日志，不重复启动队列。一个有限快照即可，正常运行时结束，不忙轮询。若`PILOT_GATE_FAILED`，检查具体错误及版本，保留原尝试后修复，不把70项阻断当成已完成。如果`ALL_FC_FITS_VERIFIED`，逐模型/回执/输入源哈希独立验收，再执行新版本的选择、冻结与调查评价。

这80项为原固定锚点集合，不能冒充完整超参数优化。BM9根及依赖问题、扩展网格、独立评价及论文结果交付仍待完成。旧925模型/表图不动；测试集已知后的计算完成扩展必须如实标注。自动任务配置未改；服务器队列本身在SSH断开后继续运行并按条件推进80项。
