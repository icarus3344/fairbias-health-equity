Gate:

Independent completion-evaluation architecture review, followed by supervisor-assigned additive survey-statistics implementation; 2026-09-18. Scope: the NEW 80 adaptive-compute BM_AE/Joint anchors. Parent retains admission, release, validation and acceptance authority.

Status:

DESIGN_AND_STATISTICS_IMPLEMENTED — REVIEW_REQUIRED. No actual S/T evaluation, model deserialization, fitting, network connection, Git staging or commit occurred in this worker. This report does not admit the 80 models or approve the new study.

Files changed:

- Added this report.
- Added `src/nhis_fairbias/benchmark/completion_statistics.py` after the parent's explicit follow-up assignment. No existing source was edited.

Commands executed:

- Read `docs/AI_EXECUTION_PROTOCOL.md` before any other action, then current `docs/reports/fairbias_cpu_replica_20260917/STATUS.md`, completion plan, historical supplemental design, selection/catalog/evaluation/inference/annual-design source and aggregate control metadata.
- Read-only Git branch/status/baseline comparisons; targeted `rg`, `nl`, `sed` and Python JSON/hash checks. Hashed all 476 backup files without deserializing prepared data or policies. Read result identity/status metadata only; no respondent rows were printed.
- Compared 80 manifest configurations to the original registration and their result identities; compared frozen training sources with the current source files and shared parent-study analysis files.
- Applied additive file patches. Ran three inline synthetic-statistics smoke commands using `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src .venv311/bin/python`; temporary outputs were automatically removed.

Permissions requested:

None. Initial read-only review authority was extended explicitly by the parent to create the statistics module. Parent owns further admission, source freeze and S/T release. No server is needed for the remaining interface work.

Tests executed:

1. Independent byte-integrity and aggregate identity checks on the local 80-model backup.
2. Source/registration/baseline metadata comparisons.
3. Synthetic statistics checks covering mean-seed EO versus ensemble EO, complete annual design, paired differences, S-infeasible and absent-model retention, endpoint correction, input immutability and output refusal.

Exact test results:

- Backup: 476/476 files, 1,673,362,569 bytes, zero hash mismatches. Completion manifest canonical seal matches its stored seal.
- Completion metadata: 80 `FC_COMPLETE_FEASIBLE`, 16 configurations, all 16 have exactly seeds `{0,7,19,37,73}`; zero config/result/data-identity mismatches. All 80 lack S metric/prediction artifacts. Prior audit records independent exact double reload on C; this worker verified the audit bytes and did not repeat those predictions.
- All 99 frozen training source files matched the current files at inspection. The only differing shared file between the completion closure and parent-study analysis closure was `catalog_reporting.py`; shared prediction, metrics and data-loader files matched. This is source-compatibility evidence, not an automatic comparator admission.
- Original registration SHA matched and all 80 config dictionaries appear in it. Parent study v3 contains 925 model references.
- The initial synthetic fixture used categories 0/1 for arm_001 and correctly failed `eligible A contains identifiers outside expected_groups`. After correcting the fixture to registered 1/2, the command exited 0 and passed 10 checks. Opposing seeds had mean EO=1 although their mean prediction has EO=0; annual design retained 24 rows/16 domain rows/df=3; paired BA=-0.5 and EO=1; S-infeasible comparison remained evaluable; absent comparison remained a row; primary/union alpha were .05/20 and .05/22; inputs unchanged; undersized families and overwrites refused.
- A subsequent parent-requested integration check passed four checks: explicit union336, registered fixed-BM tau exception retained with missing models, union-count mismatch refused, and unregistered tau mismatch refused. The only permitted unequal tau is reference .10 versus baseline None with `kind=parent_fixed_BM`, BM method and FIXED_ABLATION/NO_VALID_FIXED_ABLATION status.
- Optional-library import warnings named TensorFlow, inFairness and rpy2; these libraries were not needed by the tested statistics. No pytest suite was claimed; parent/independent test worker owns integration coverage.
- Fourteen inherited root files equal the protected tag. `.gitignore` equals HEAD and differs from the protected tag, consistent with the pre-existing discrepancy documented in STATUS. This worker did not modify it.

Input hashes:

- Protocol: `1a3d38929d65493f8352d6b46d32f65fa836502d08a3c2d2704dd713e5c3beac`.
- Completion plan: `865f28ba00cc89711125c2292ecfeab18b3052410114c895a3fc5ffd8b13b509`.
- Historical supplemental design: `dd60d5e8943f8dc3e19a4746958a89f6ed7d79bd95e32ff2639d852702429a0b`.
- Original registration: `340cd60f6ab0d5919e2ac7bc22b62bdb3cc758faaa986650abdb3978b964732a`.
- Backup `control/files_manifest.json`: `fe1e5c778c65758e551a7c1be4f33dec24bb07270eabdde360c0846cfac4df17`; `control/audit.json`: `977b1b5dce782eea090c63599af9adfed099b3eef331e5e54b0015209405ce5a`.
- Frozen completion `control/manifest.json` file SHA: `e78f7d0aa9ac036ce08883255ad47661a033c13c15333bf161639905f83796a8`; canonical content seal is separately `6607256f5efa027e0c954e7e0a0021d886074bf6dbfde0a52f1e5c5ba69ba0f0`.
- Parent `completed_main_analysis_backup_20260917/snapshot/artifacts/nhis/analysis_catalog_20260917_064230Z/main_analysis_v3/study_freeze.json`: `b9531ebba6c95e66489255eb0a232ce2a67b585e48799673d8d0c6dad1a10b1e`; bound selection SHA: `ef11fe4864e0fc5d497c3c5731d1d633ecf16f2b038cca2ab03d661906b87838`.
- Inspected source SHAs: `catalog_evaluation.py` `6bf830e4a387776002a8731daeaee803f5a03920023cf65fa50a9db42349c103`; `catalog_selection.py` `4d7ce4e60b950b02f70b2cce3e5530ee998eab8df6cb3c314180636f2e32ba5e`; `experiment_selection.py` `664633c20cfaf1225c1d2398fb3ebe2a2a053531622004e1bafb94c9a55ba64f`; `result_catalog.py` `889de299448e0c725012bb2cc657975b90122d41cb2f10a29e4918c28cc4a79a`.

Output hashes:

- `src/nhis_fairbias/benchmark/completion_statistics.py`: `cfde4e6e900388b777d9674124bb0c74a90c6014756a9a240a3881799d97c236` at handoff. Parent must recompute after any integration edits.
- Report SHA is supplied with worker handoff; no self-referential hash is embedded.

Row counts:

- New study denominator: 2 methods × 4 arms × 2 backbones × 5 seeds = 80; 16 fixed configurations. Each method has 40 models.
- Historical study remains 925 frozen models. Neither new models nor formerly unsuccessful jobs replace any historical selection, denominator or result.
- Each new cell has one fixed configuration, epsilon_ratio=1, unweighted training, LR C=1 or GBDT 100 trees/depth 2. Three existing tau values (.05, .10, .20) produce feasibility labels, not three distinct fitted policies or a newly tuned grid.

Assumptions:

The new analysis is explicitly a **post-test-known adaptive-compute completion extension**. F/C completion and geometric feasibility do not establish S EO feasibility or favorable T performance. `FAIRBIAS_FULL_METHOD_COMPLETION_20260917.md:23–43` supersedes the old design's numerical 6/8 AE and 2/8 Joint restrictions for these NEW artifacts, while preserving those historical branches unchanged.

Unresolved issues:

**Required admission architecture and source paths**

1. Build a new metadata-only result index/manifest with a distinct branch. Bind original registration, all 80 original job anchors, frozen99 manifest file SHA AND canonical seal, explicit remote-to-local paths from audit, result/policy/progress hashes, prepared hashes, queue receipt, prior audit and parent-study/selection hashes. Preserve unknown historical process exit codes as unknown; independently audited artifacts need not be falsely rewritten as exit=0. No latest/success directory fallback. Preserve all 16 complete-seed denominator rows.
2. Existing APIs are deliberately incompatible. `catalog_selection.py:153–189` prohibits adaptive BM_AE/Joint replacement in main analysis. `result_catalog.py:297–339` requires finite old execution budgets and knows only old recovery policies. `catalog_evaluation.py:32–51,159–256,386–421,563–573` hard-binds the main branch, BM reference and family20. Do not relax/relabel them to admit completion artifacts.
3. Completion artifacts are **bare `policy.joblib`**, created by `run_nhis_fairbias_completion.py:188–200`; old models are dictionaries with policy/preprocessor/config/seed/data identity (`catalog_evaluation.py:427–440`). A new loader must validate the policy's fitted/calibrated state, adapter mode/seed/config/budget provenance and frozen threshold against the bound manifest/result, then use the semantic feature interface. An in-memory `{'policy': policy}` can call `catalog_inference.predict_frozen_bundle` after independent validation; do not invent a historical `model.joblib` or receipt.
4. Training's `source_hashes()` scans every package Python file and `load_manifest()` requires exact closure equality (`run_nhis_fairbias_completion.py:40–47,104–118`). Adding this statistics module makes the live checkout a different closure. Validate the original manifest against its archived runtime, and bind the additive evaluator's executing closure separately. Never reseal the old training manifest to include new code. Likewise, main-study validation cannot be rerun against altered live sources or unresolved remote paths as if it recreated the historical freeze.

**Selection and T contract**

5. Run S predictions only after metadata admission; record actual S provenance/role/year, prediction identities and immutable output hashes. Use existing `aggregate_configuration`/`select_aggregate` semantics (`experiment_selection.py:14–39`): complete seed set, mean seed BA/EO, EO≤tau, BA then EO then complexity then candidate ID; least-EO boundary if infeasible. With one config per cell, S supplies feasibility, not useful ranking. Existing C thresholds remain frozen; do not call fitting/calibration or retune on S/T. No successful-seed filling or replacement of a failed new seed with an old seed.
6. Freeze selection, full endpoint registry, complete arm modelsets, environment/source closure, inference policy and exact T provenance. Require a new branch-specific supervisor release bound to those hashes before any T I/O. Parent T identity is `official HHX + survey year`, raw2024 SHA `31c5be8d630c13f6d0a232831a79654de7038d97fc4f33d803d8d2c84705c9d5`, feature registry `9fb42eb3b14c9d80b56476b1cbac5e4981843e020a00d7918702676026975018`, study registry `d0fa6b12b8fae5d77ddbb75aef91de9294c1c5d4b93fb5fbf3990c560dbbd964`. Hash checks precede model/T decoding and repeat before COMPLETE manifests. Output roots and shards must be fresh and immutable.

**Comparators, endpoints and multiplicity**

7. Reusing old selected comparator predictions avoids refitting. First verify parent freeze/selection, per-arm shard/manifest, exact model ID/seed membership and NPZ hashes; require identical arm, target, categories, training-weight flag, tau, annual/domain membership and row ordering. Old NPZ files omit record keys (`catalog_evaluation.py:480–491`): row order must be justified by the same hashed T bytes/registries/loader and deterministic ordering, or reconstructed and bound internally. Matching array length alone is insufficient. Recompute new paired intervals from per-model q under common PSU factors; old summary intervals cannot be subtracted or reused as new-family intervals.
8. Frozen comparator methods retain their original output contract: BA/DP/EO use decision q; AP/AUROC/Brier require event-risk p. q-only methods have unavailable risk metrics, with untouched-base risk clearly separate. Unequal registered seed counts across methods are legitimate: average each complete registered seed set; do not force seed pairing or duplicate a deterministic comparator. Survey pairing concerns shared respondents/PSUs. Training seed SD/range is not survey SE.
9. Parent's registered policy, `docs/plans/FAIRBIAS_COMPLETION_EVALUATION_POLICY_20260918.md`, is defensible as an extension fixed before new S/T: Joint reference, arms001/003, LR, unweighted, tau=.10, five old comparator slots REWEIGHING/LFR/EG_DP/EG_EO/TO_EO, two endpoints BA/EO = **20 endpoint slots**. Define delta as Joint−comparator; positive BA and negative EO favor Joint. Primary Joint status must not switch to BM_AE after viewing results. Secondary registry: both new methods against nine tau=.10 comparators across eight arm/backbone cells (144 pairs minus primary10), Joint−BM_AE8, and new methods−fixedBM16, totaling **158 pairs/316 endpoints**; union **336 endpoints**. Unsupported/unavailable slots remain in m. Separate families support separate claims; do not claim joint 95% coverage across them. The new module emits union-family bands for a conservative combined interpretation.
10. Fixed-anchor component effects require matched fixed BM anchors, estimator/epsilon/seed compatibility and complete baseline cells. Comparing a fixed Joint anchor against S-tuned BM is a benchmark comparison, not a clean isolated AE effect. Wider Joint/BM_AE grids and unresolved BM9 roots/dependents are separate scope, not a reason to conceal or relabel the current 80-anchor analysis.

**Implemented statistical contract**

11. `completion_statistics.evaluate_statistics(...)` accepts one arm's complete frozen selections, annual-expanded q vectors, per-model weighted/unweighted metrics and explicit contrasts/global endpoint counts. It calls the existing annual-domain normalization, `survey_linearization.linearized_survey_inference`, `survey_batch.bootstrap_metric_arrays`, and existing summary/projection helpers. Full annual strata/PSUs include zero-domain contributions; singleton/undefined strata/rates remain unestimable. Bootstrap B=2000/seed20260914 and its allocation guard are retained.
12. Mean seed metrics are primary. Mean-q is used only for BA and paired BA, where linearity holds. EO/DP use mean per-seed gaps and simultaneous per-seed rate projections across ALL frozen models. Common PSU replicates give descriptive gap intervals; no normal max-gap p-value is invented. Nominal bands, family-adjusted bands and union-adjusted bands are distinct. S-infeasible pairs remain present with `feasible_on_S=false`; missing models remain status rows. AP/AUROC/Brier are descriptive mean-seed point/spread/delta outputs; this module does not provide design CIs for those risk endpoints.

**Genuine blockers versus implementation work**

- Before scientific output: parent must finish/test additive admission+S+freeze+release+prediction/merge code, freeze the exact endpoint registry, and verify archived comparator row-order/provenance bindings. These are real prerequisites, with no need to ask for idle remote compute.
- No metadata evidence here requires refitting the completed80, changing existing frozen source or adding a GPU. Wider tuning/new-year validation is a different scientific scope. A previously seen 2024 T cannot become a new blind test through another freeze.
- Mandatory denial tests should cover main-branch masquerade, altered seal/backup/model/receipt, wrong config/seed/source, missing/duplicate seeds, absent release, changed T/row order, mixed p/q, changed endpoint denominator and omitted/duplicate contrast rows. Parent and independent test worker retain these integration checks.

Git diff summary:

Worker added exactly the two files listed above. The checkout was already extensively dirty on `research/nhis-fairbias`; none of those prior changes were staged, reverted or claimed as this worker's work. No baseline edits.

Proposed next step:

Parent independently review/test the new statistics module and integrate it with separately frozen completion admission/evaluation. Approve a concrete endpoint registry before new S/T; then verify S outputs and new release before evaluating T. Preserve original925 outputs, prior failure history and all earlier supplemental branches. This worker does not self-approve.

STOP — waiting for Codex review.
