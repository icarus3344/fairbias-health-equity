# FairBias 并行分析执行与监督验收

截至 2026-09-17 15:10，Asia/Shanghai。分支 `research/nhis-fairbias`，HEAD `67e6659fa65249a8842e34af5d8969629efe4bca`。本报告验收执行证据与分析基础设施，不宣称已完成模型比较或论文结论。

## 1. 本轮已交付

| 工作 | 实际结果 | 接纳边界 |
|---|---|---|
| A1 跨运行索引 | 8 个来源、9,166 条运行记录，覆盖全部 8,054 个注册任务；54,886 个唯一文件完成哈希核对，0 项完整性问题 | 原始/替代/待定/预算敏感性身份分别保留；没有选择模型或解码性能指标 |
| A2 覆盖与失败汇总 | 原版、FRAPPÉ、LFR、EG、BM 和 Joint 的执行状态分版本汇总 | 失败是有效终结记录，不能视为有效模型；没有按结果优劣拼接版本 |
| EG 兼容检查 | 1,536 个原版 EG 任务中，1,513 个有效模型的已保存 S 决策概率通过端点修复兼容检查 | 仅支持 `COMPATIBLE_STORED_S_Q_ONLY`；不证明未来 T 输出、跨硬件训练等价，也不自动授权合并或选模 |
| A3 选择接口 | 新增完整注册任务到运行来源的显式映射、预算/版本约束、冻结产物及模型位置解析 | 58 项新合成测试由监督在本地和 CPU 分别通过；尚未执行真实 S 选模，跨运行 T 评价接口仍待完成 |
| A4 论文方法草稿 | 已形成方法与统计口径草稿，保留实证数量和结果占位 | 不能作为最终结果段落；说明 2024 是回顾性跨年评价 |
| A5 本地备份 | 原 GPU 归档 44,909 文件、5,468,502,929 字节本地校验通过，0 差异；另启动 CPU 已结束运行的 11,299 文件、899,966,466 字节增量备份 | CPU 增量包在本报告时仍传输中，不包括正在运行的 BM→AE40 或未来最终分析产物 |

原运行有 7,643 条实际记录，未启动的 411 个旧 FRAPPÉ 任务由已验收的完整修正版本覆盖。原版已跑的 69 项 FRAPPÉ 仅作为历史记录；正式修正版本为 480 项。9,166 是尝试记录数，不能称为独立注册任务数或成功模型数。

索引及小型汇总已经同步到本地证据目录；约 21 MB 的完整 `catalog.json` 与 `jobs.jsonl` 保留于 CPU 的 `artifacts/nhis/analysis_catalog_20260917_064230Z/catalog_v1/`。下一次修改版本接纳决定应生成新清单和新目录，不覆盖 v1。

## 2. 已验证执行状态

以下来自本轮完整性通过的目录快照，均未读取方法比较性能。

| 方法/版本 | 有效执行 | 其余终结状态 |
|---|---:|---|
| FairBias-BM 原预算 | 3,322 | 22 预算耗尽，176 失败 |
| BM MDS 预算敏感性 | 22 | 0；不可静默替换原预算 |
| BM→AE 原预算 | 19 | 16 预算耗尽，5 超时 |
| Joint 原版 | 0 | 2 预算耗尽，38 超时 |
| Joint 严格精确版本 | 13 | 20 预算耗尽，5 调度运行失败；后 5 项的既有终结验收识别为外部时限停止 |
| LFR 数值修复完整版本 | 960 | 0；原版 960 预算失败另存 |
| EG 原版 | 1,513 | 23 失败 |
| EG 数值修复 | 23 | 0；v1 索引仍标待兼容接纳 |
| FRAPPÉ 修正完整版本 | 480 | 0 |
| FairGBM / fairret / OxonFair | 480 / 80 / 384 | 0 |
| Reweighing / TO / Unmitigated | 96 / 96 / 312 | 0 |
| 三种几何对照 | 各 10 | 0 |

完整种子失败会使相应配置无法参与既定选择；此表中的有效任务数不是可比较配置数。BM 特征耗尽也不意味着所有潜在搜索路径均不可行。

## 3. 新 BM→AE 预算敏感性正在执行

长时限试运行通过 F/C 完整性与精确重载验收后，监督登记并启动**全部 40 项** BM→AE，而非只补跑原先失败的 21 项。原先 19 项成功任务也重新拟合，避免混合预算和选择性修复。

- 运行控制器 PID `696466`，14:48:07 启动；15:05 的实际进程检查确认子任务已消耗约 1,021 CPU 秒。
- 采用独立版本 `236c7bbe973a2cdaccebdc5c20c634536d9c715ba0c82622e5750981d3c6a7f1`，源码快照 90 文件，旧 89 文件不变；新分析文件不进入训练身份。
- 严格条件性 AE 上限 10 → 40，仅遇到精确 AE cap 才从同一 seed 全新重跑；外部总时限 7,200 秒，每任务 4 GiB。其他每次拟合内部预算保持原登记，MDS 不自动重试。
- 全局最多 28 槽、48 GiB 工作预算，实际实例 32 核 / 60 GiB，预留 12 GiB。
- 本地和 Linux 分别独立通过 36 项测试；不承诺所有任务成功。两项原 MDS 预算问题仍可能保留失败。
- 实验保留为独立预算敏感性，FairBias-BM 仍是主方法。本轮未释放更多 Joint 重跑。

登记说明：`docs/plans/FAIRBIAS_BMAE_BUDGET_SENSITIVITY_20260917.md`。控制与验证材料：`fairbias_recovery_20260917/evidence/bmae_sensitivity_control/`。

## 4. 独立测试与代码审查

- 结果目录 45 项 + EG 检查 14 项：Linux **59 passed in 6.82s**。EG 的 10 项监督追加测试覆盖真实失败返回码、原版/修复身份、必要文件、路径安全及非 EG 混入等情形。
- 新跨运行选择接口：监督本地 **58 passed in 10.63s**，Linux **58 passed in 12.60s**。代理另外报告目录加选择联合 **103 passed in 16.57s**，未将代理报告代替监督测试。
- 生产评价到汇总、论文导出的生成数据链：监督 **7 passed in 3.00s**。q-only 方法的风险指标保持不可用，untouched-base p 单独保留。
- BM→AE 新 worker/queue：监督本地 **36 passed in 13.63s**，Linux **36 passed in 16.02s**。首次本地调用缺少必要的 `PYTHONHASHSEED=0`，夹具拒绝启动；补齐环境后通过，未把这次启动错误称作生产算法缺陷。
- 两项初期审查判断已撤回：生产链已有 `evaluation_manifest_sha256`，也已输出 `untouched_base_*`。真正未完成的是跨运行冻结、T 重载及导出身份的贯通，不能凭局部读取误判为字段缺失。

新增选择接口保持既定种子均值、阈值、复杂度与 ID tie-break，保留 NOT_SUPPORTED 与失败分母；主分析禁止预算敏感性替代。EG 跨版本需要另行签署绑定目录、注册表、逐任务映射和证据哈希的监督兼容清单。现有 EG audit 本身不能作为该清单。

## 5. 证据位置与哈希

- `fairbias_recovery_20260917/evidence/catalog_terminal_verification_20260917.json`：目录终结证据及全方法计数。
- `fairbias_recovery_20260917/evidence/analysis_catalog_small/`：来源清单、汇总、空完整性问题表。
- 完整远端目录 SHA256：`de059f22e8cf98e7b6c33749129bd6b4d8446d386660b325ce731b0ec63d516b`。
- `fairbias_recovery_20260917/evidence/eg_compatibility_report_20260917.json`：SHA256 `321d977945e005e6fd7b6810ef867e8ea25ac046a9729daf394da18ffb2c134c`。
- `catalog_selection.py`：`b62868e5f4ad07d825891b398ad505f471209763f23fff14dbb8d8f9c35d7806`；测试：`0201b5dda910b864c3df28749d544882b6cfbc85147bdd392e4ba92b9855dd54`。
- 原归档本地最终证明：`artifacts/nhis/source_retirement_20260917/control/local_from_cpu/verification.json`。
- CPU 新增备份 PID `94139`；清单 SHA256 `2ae479a8ec032f9457aaf61f9ab2b07e861393a0d5d22cbdb1b16ef191891100`。状态和最终证明位于 `artifacts/nhis/completed_cpu_backup_20260917/control/`。传输失败不能标记完成，服务器保留原件。

## 6. 保留问题与下一步

1. 新 40 项 BM→AE 终结后，独立验收模型/收据/预算，纳入新的敏感性清单。失败如实保留，不无限扩预算。
2. 完成 EG 版本接纳与统一数值安全推断策略；原版 q 的兼容证明不能取代新的 T 接口测试。
3. 在任何比较排名之前，解决预声明扩展状态、方法版本和完整注册来源映射；完成跨运行 T 评价及论文导出对冻结研究和分析源码的绑定。旧 `complete_nhis_study.py` 不得直接启动。
4. 冻结后才按既定规则在 S 选择，再释放固定模型的 T 评价、设计统计及图表。单个冻结分片先测量耗时和内存，再分配其余分析资源。训练与统计尚未全部完成，当前不给无依据的总交付 ETA。
5. 增量备份完成后校验；最后还要备份新 BM→AE 及最终分析产物。CPU 退租前必须具备租用盘外完整、可验证副本。

另有历史治理差异：14 个继承文件与保护标签逐字节一致，但 `.gitignore` 已在既有提交 `b595e59b67d8781a3dca41199487909fdb9ed42d` 增加演示文稿/PDF 忽略规则；当前工作树与 HEAD 的 `.gitignore` 一致。本轮未修改或回退它，不能声称 15 个保护对象全部等同标签。此项与本轮已验证的训练/分析源码身份分别记录。

本轮只新增分析、恢复、测试、文档及证据；既有工作树修改保留，无 staging/commit，无新 T 访问。沿用两小时续办，停止主动轮询；服务器训练和本地传输均为独立进程。Mac 休眠不影响已启动的 CPU 训练，但会影响本地备份和及时续办。
