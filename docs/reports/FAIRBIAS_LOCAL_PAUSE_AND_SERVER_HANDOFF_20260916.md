# FairBias 本地暂停与服务器迁移交接

状态：**PAUSED_BY_USER_FOR_SERVER_MIGRATION**。

用户于 2026-09-16 明确要求暂停当前本地任务，待获得服务器资源后再协助迁移。本次指令覆盖此前的本地自动完整推进安排。不要自动恢复本地训练、候选选择、2024 评估或结果生成；等待用户提供目标服务器信息后，在目标环境核验并开始新的执行批次。

## 已执行并核验

- 已停止后续完成控制器 PID 23343、训练调度进程 PID 19210、当时正在训练的子进程 PID 73244；对应防休眠进程已退出。
- 最终进程检查未发现本项目仍在运行的训练、自动评估、旧串行 benchmark、契约测试或 pytest 任务。三个协作子代理均处于已完成状态。
- 运行目录：`artifacts/nhis/benchmark_codex_20260916_014500/`。
- 已结束并具有回执的任务：**1,356 / 8,054**；其中 **1,036 VALID**，**320 BUDGET_EXHAUSTED**。
- 所有已完成回执中的文件 SHA-256 均匹配；原完成准入所绑定的 **85 个文件均保持一致**。
- `selection_freeze.json`、`complete_study_freeze.json` 和 `evaluation_T/` 均不存在；本批次尚未启动新的 2024 模型评估。
- 当前分支 `research/nhis-fairbias`，HEAD `67e6659fa65249a8842e34af5d8969629efe4bca`。保留原有未提交修改，不暂存或提交。

## 中断任务的处置

任务 `de29dfc6bb0e821e8437_s0` 在用户暂停时尚无结果/回执。其原有 `job.json` 和 `worker.log` 保留原位并记录哈希；在暂停清单中标记为 `INTERRUPTED_BY_USER_NOT_A_MODEL_FAILURE`。

本次暂停终止了操作系统进程，释放计算资源；没有可供跨机器继续执行的进程内训练检查点。迁移时需要重新执行这一次未完成训练，使用新的执行/尝试身份。不得将其记为算法失败、预算耗尽或有效结果，也不得删除原目录来制造未曾执行的记录。

此前 320 项 LFR 预算退出保持原状，实际对应 40 次原始表示学习未收敛及 280 个缓存依赖配置。其原因及需要单独登记的训练预算验证见 [进度检查](FAIRBIAS_PROGRESS_CHECK_20260916_0850.md)。

## 暂停证据和源码保存

目录：`artifacts/nhis/benchmark_codex_20260916_014500/pause_events/20260916_011022Z/`。

- `processes_before.json`、`process_stop_receipt.json`：进程身份及停止记录。控制器和调度器退出时出现瞬时进程状态变化，严格身份检查使两次停止脚本提前退出；随后逐项重新核验，确认并停止唯一剩余训练进程。最终没有遗留运行或挂起进程。
- `pause_state.json`：完整暂停状态、各方法计数及中断任务身份。
- `run_artifact_manifest.json`：已完成结果、回执、准备数据、表示缓存和运行登记等文件的哈希清单。
- `admitted_files_verification.json`：85 项冻结文件核验记录。
- `source_and_plan_snapshot.tar.gz`：240 个源码、配置、测试、治理与计划文件的本地快照，约 0.94 MiB。
- `source_snapshot_manifest.json`、`working_tree.patch`、`git_status.txt`：源码哈希及暂停时的工作区状态。

快照用于保存当前实际实现，不能以旧 HEAD 的普通克隆替代。该源码包不是包含数据和所有依赖的完整迁移包；本地数据、官方依赖源码/构建材料、历史产物仍在原目录保存。`live_status.json` 已更新为暂停状态；原训练结果与回执未改写。

## 服务器就绪后的工作

1. 获取操作系统与架构、CPU/内存/GPU 配额、登录方式、调度系统和存储位置。
2. 根据 [服务器资源申请与迁移清单](../plans/FAIRBIAS_SERVER_RESOURCE_REQUEST_20260916.md) 重建 Linux 环境，验证原生依赖、跨平台内存单位、模型保存与重载以及调查统计。
3. 为并行作业增加独立调度、共享表示依赖管理和缓存写入保护；不能同时启动多个现有驱动共同写同一目录。
4. 保持数据划分、随机种子、指标与方法定义可追溯，独立登记 LFR 预算修正和可选 GPU 设备适配；不得依据 T 排名调参。
5. 通过跨平台检查后决定哪些历史产物可复用，为服务器执行建立新身份、回执和环境清单，再恢复剩余研究工作。

当前没有定时自动恢复安排，也没有远程传输或服务器作业提交。
