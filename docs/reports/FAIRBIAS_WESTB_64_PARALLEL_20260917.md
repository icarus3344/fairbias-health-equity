# FairBias 64-way resource adjustment — 2026-09-17T22:53:16.808499+08:00

Supervisor accepted the scheduler transition and resource measurement, not full-family research results. User explicitly asked to use remaining cores. Before adjustment, the 64-core quota had 56 runnable single-thread fits, 21 queued, three completed F/C jobs, about 87.3% CPU and 14.9 GiB memory. After adjustment, CPU measured **99.25% over five seconds**, with **64 live distinct workers, 13 queued, three completed F/C jobs**, 16.64/120 GiB memory and 48.34 GiB free disk. No end-to-end speedup or ETA is inferred from this interval.

New controller PID 2561 replaces 2303. The previous controller's source is preserved. All 56 running model processes were adopted without signals/restarts; their PID/start-time and output paths were independently checked after takeover. Eight never-started registered jobs were added. Maximum concurrency is 64; the existing 80% memory/5 GiB free-disk guards remain. No wall-clock training timeout or scientific parameter change was introduced. More concurrent CPU-bound work is not indicated by the current utilization.

Current control directory: `/root/autodl-tmp/fairbias_completion_parallel64_20260917`. Its `state.json` and eventual `queue_receipt.json` are authoritative. Earlier controller state files are historical. New jobs write beneath its `jobs/`; adopted fits continue writing at their original paths, explicitly mapped in `handoff.json` and live state. Original three completed receipts retain their prior acceptance category. Inherited exit codes remain unknown and must not be represented as observed zero.

Frozen runtime root remains `/root/autodl-tmp/fairbias_completion_v2_20260917`; the 99-file manifest seal remains `6607256f5efa027e0c954e7e0a0021d886074bf6dbfde0a52f1e5c5ba69ba0f0`. New external controller SHA-256: `e209d25fbbb19419115890455ded5323c92975703dec6c090ae2b1816beafb4f`. Handoff script SHA-256: `0b39f48e7062e8f8ef078f73a8ab9ff70285dd4142e09ff8acddff0b9ff7087b`. No frozen training source, model hyperparameter, input, seed or split changed; no S/T performance used. The same global owner lock prevents duplicate scheduling.

New v2 controller regression checks passed locally **8/8 in 0.08 s** and on Linux **8/8 in 0.55 s**. The integration case preserves 56 inherited tasks across two output roots, three completed receipts, and dispatches the remaining 21 exactly once at cap 64. Local evidence and downloaded hashes are in `artifacts/nhis/full_method_completion_20260917/westb_35430/parallel64_control`. Independent receipt verification binds controller source, handoff, original state, transition, and measured process identities; `download_verification.json` records the transferred file hashes.

Next: one bounded state/receipt check when needed. On completion independently audit all 80 outputs and model reload/provenance before separate frozen selection/evaluation. BM root failures and any expanded Joint grid remain outstanding; these 80 anchors do not close all paper work. Server controller runs independently of Mac/SSH. App automation remains paused. No Git staging or commit.
