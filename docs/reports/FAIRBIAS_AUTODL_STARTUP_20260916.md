# AutoDL 迁移与并行启动验收

状态：**CPU_DEVELOPMENT_RUNNING — 已启动服务器开发阶段，尚未完成论文比较。**

2026-09-16 16:00 CST 左右，正式队列已在服务器后台运行，控制器 PID 6865。任务不依赖本机 SSH 会话持续连接。本地训练保持暂停，旧运行与结果保留，未进行 Git 提交。

## 运行与资源

- SSH 别名：`fairbias-autodl`。
- 项目：`/root/autodl-tmp/fairbias`。
- 新批次：`artifacts/nhis/benchmark_autodl_20260916_075038Z`。
- 日志：该批次的 `full_cpu_queue.log`；动态状态：`parallel_scheduler_live_status.json`；逐任务事件：`parallel_scheduler_events.jsonl`。
- 实际 cgroup 配额：16 CPU equivalents、80 GiB；购买页面标示 15 核。不可用宿主机显示的 192 核/1 TiB 作为并行依据。
- 先用 8 并发完成真实试跑，再启动 **12 并发**。每个作业 1 个 BLAS/OpenMP 线程、4 GiB RSS、1800 秒上限；整体进程 RSS 上限 64 GiB。单任务科研预算未修改。
- 启动后 5 秒采样实际使用约 **11.79 个 CPU 核**；cgroup 总内存约 **16.41 GiB**（包括文件缓存）。数据盘约使用 11/50 GB。12 并发是当前留有余量的运行设置，不是已经证明的全局最优值。

## 独立核验

1. 22 个开发输入/可移植依赖文件与 186 个源码/配置/测试文件分别经过逐项 SHA-256 校验；实际源码包含未提交工作，未以旧 Git 克隆替代。
2. Linux 与暂停的 macOS 批次在原始数据来源哈希、候选配置矩阵和各 F/C/S 分区人数方面一致。Linux 使用新执行身份，没有导入旧 macOS 模型。
3. 新建 Python 3.11.16 环境，保留 numpy/pandas/scipy/sklearn/fairness 的登记版本。PyTorch 为 2.13.0+cu126；RTX 4090 D 的实际 CUDA 矩阵运算验证通过。主环境 `pip check` 通过，镜像安装的 30 个包下载哈希与官方 PyPI 元数据匹配。
4. FairGBM 从保留的 0.9.14 源码重新以 Linux CMake/OpenMP 编译，原生库确认为 ELF；合成拟合、保存与新进程重载测试通过。
5. 服务器主测试：**153 passed，1 deselected**；随后并行/资源/工作进程专项：**19 passed**。两组有重复测试，不能相加宣称 172 个独立测试。未完成的 FRAPPE 新进程检查没有被计作通过。
6. 另用实际生产 worker 完成 3 个合成并行任务，全部 VALID，并验证两个 FairBias 预测器共享同一个表示模型。
7. 真实试跑：24 个 UNMITIGATED、16 个 FAIRBIAS_BM，全部 VALID。后台控制器只有在这 40 项回执和哈希检查通过后才启动 12 并发队列。07:59:41 UTC 的进一步核验已有 62 个有效结果、零失败；这是启动时快照，不是最终进度。

## 调度修复

- 将 macOS/Linux `ru_maxrss` 统一为字节，记录 CPU 时间与线程环境。
- 新增单个运行目录的排他锁、共享表示单写入调度、原子回执、重启时结果哈希/身份核验。
- 监控进程树 RSS；监控失败时停止调度。保持源文件和方法预算绑定，单独记录各次并行执行设置。
- 中断的未完成尝试保留，需显式恢复；不会将用户中断自动当作模型失败，也不会覆盖原目录。

## 当前队列范围与未完成项

完整登记仍为 1922 个配置、8054 个种子任务。当前 CPU 队列覆盖其中 **6614 项**（包括已通过的 40 项试跑），包含 FairBias BM、BM+AE、joint、几何消融，传统公平性方法、OxonFair、FairGBM、fairret，以及对应预测骨干。

- **FRAPPE 的 480 项暂未启动**：独立 Linux TensorFlow 2.14.0 仍在下载。完成安装后需要依赖检查、合成拟合、跨进程重载和种子复现验收；不得把“有解释器”当作已可运行。
- **LFR 的 960 项暂未启动**：须先解决已知函数计算次数预算不足，形成仅使用训练数据的独立预算修订。历史 320 个预算退出保持原状，不计作 FairBias 的优势。
- **GPU 尚未用于模型训练**：已验证 CUDA 硬件，当前神经网络适配器仍按原登记使用 CPU。后续 GPU 适配需独立验证并登记，不把“安装了 CUDA”当作已加速全部方法。
- 当前服务器仅有 2022/2023 原始数据；没有启动候选选择、2024 测试评估或论文结果生成。调查统计的最终运行与论文推断仍属后续阶段。
- 环境中的 R `survey` 独立交叉核验尚未迁移；现有 Python 调查统计契约测试通过不能替代这一步。

## 证据位置

本地：`artifacts/nhis/autodl_migration_20260916T064428Z/`，含输入/源码清单、Linux 注册清单、跨平台注册比较和 `startup_evidence/`。已从服务器拉回 22 份启动证据并重新验哈希。服务器环境/安装日志位于 `/root/autodl-tmp/fairbias_deployment/`。

启动证据压缩包 SHA-256：`33985039003b4d12f5ec934bc9f6af2acb1e28390d3d737e1856bbbf66ed015e`。

恢复时先读取后台队列状态、退出回执与逐任务回执，再决定后续阶段；不要并行启动旧串行驱动写同一运行目录，不要覆盖源码来改变已经注册的在途实验。
