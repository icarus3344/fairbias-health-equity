# 正式评价中断：调查域标记类型修复

2026-09-17，主管检查用户请求的实时进度时发现中断。上一份“已启动”记录只证明当时进程运行；不能用它推断评价仍在继续。

## 实际进度与根因

主评价 v1 于 17:47:06 启动，17:50:34 因 Arm 001 错误终止，用时 208.22 秒。18:14:43 实查时控制进程与评价进程均已退出，四个臂没有完成回执，预测文件数量为 0，其他三臂未启动。训练结果已完成并保留；本次失败发生在分析阶段。

失败位置是 `catalog_evaluation.py` 将第一模型预测扩展回完整年度调查设计时，调用 `AnnualSurveyDesign.expand`，因 object 类型的索引触发 `IndexError`。原冻结加载器 `data_contracts.py` 对可空整数列执行 `isin` 并将 pandas 可空布尔结果直接 `to_numpy()`，在服务器 pandas 2.1.1 中得到元素为真/假的 object 数组；pandas 可用该数组筛选，而 NumPy 数组不能直接用它作布尔索引。

这不是模型训练失败，也不是依据测试集效果进行选模。中断前年度数据及第一个模型已加载，且已计算第一份模型输出，但尚未写入预测文件、性能汇总或完成统计。不得称为“尚未接触 T”，也不得把未完成输出当正式结果。

## 修复范围

保持原注册训练源码和数据加载器不变，仅在分析入口增加 `_normalized_annual_design(T)`，供预测扩展与统计计算共同使用：

- 只接受原生布尔数组，或全部元素确为 bool / NumPy bool 的 object 数组。
- 缺失值、字符串、数字、二维标记一律拒绝，不能通过真假值强转引入新的排除规则。
- 核对年度数组长度、入组人数，以及选中记录的顺序、分层、PSU、权重与分区完全一致。
- 返回新设计对象，布尔标记内容不变，原设计对象、年度样本与域外 PSU 均保留。

唯一科研分析源码变更：`src/nhis_fairbias/benchmark/catalog_evaluation.py`。

- 旧 SHA：`b58bf055121be2a3f8430bc1014cd7ac8d119f1da3d7f75aade826af0486c4b1`
- 新 SHA：`6bf830e4a387776002a8731daeaee803f5a03920023cf65fa50a9db42349c103`

旧源码已保存在本地 source_history 及远端 v1 的完整冻结源码副本中。原失败目录与旧 study / release 均保留，不能覆盖或伪装成成功。

## 验证与续跑要求

既有推断、评价与报告测试：本地 104 passed / 29.32 秒；CPU 实际混合推断环境 104 passed / 38.65 秒。另补可空类型生成数据回归，覆盖真实加载器、零贡献 PSU、非法标记拒绝、错位拒绝，以及 object / bool 两种存储下完整评价与 2000 次重采样数组的精确相等。最终新增测试与启动凭证见本报告后续记录及 evidence。

后续技术续跑必须重新构建 study v3，并要求与旧 study 的全部字段逐项相同，唯一允许差异是该分析文件的 SHA。原 selection 字节、925 个模型、种子、对比方法、参数、输入及统计规则必须原样保留。这个条件满足后，主管预授权新 release 和全新 `main_evaluation_v2` 输出；不重新训练或选择模型。

CPU 仍需要保留。新版完成之前不能承诺具体关机时间；最终停机仍要求主分析、补充分析、结果验收及新产物本地备份完成。

## 修复验收与启动记录（18:23）

新增回归共 10 项，主管独立复跑本地 10 passed / 3.04 秒，CPU 实际混合环境 10 passed / 6.20 秒。连同既有测试，两端各 114 项通过。测试文件 SHA：`a5d6f7d46b6475e30d2dd225258e856111e8b5e620b7b883d39295ee62f2b155`。真实加载器生成的可空列路径及其对象型标记得到直接覆盖。

**18:23:07** 启动技术续跑进程 **772021**。它先完整重建并逐项比对 study v3，唯一允许变化为上述源码哈希；满足条件后根据主管明确政策生成新 T release，启动 `main_evaluation_v2`，继而按原计划评价四臂、合并、导出。若等价核验失败则留下失败回执并停止，不自行选模或改变规则。

条件释放政策 SHA：`c45a8de728b459579aad230bf8b47b9753c0d1befd2eb33c96da0ff9a30a5bc5`。控制目录为 CPU 分析根目录下 `control/main_preparation_v3/`，冻结输出 `main_analysis_v3/`。本轮启动表示修复后的流程恢复运行，不表示已完成四臂评价。

启动与测试证据：`fairbias_recovery_20260917/evidence/domain_mask_continuation_launch_20260917.json`、`domain_mask_continuation_postlaunch_20260917.json`、`domain_mask_new_tests_linux_20260917.json`、`domain_mask_linux_existing_tests_20260917.json`。
